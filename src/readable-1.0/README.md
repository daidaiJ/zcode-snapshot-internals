# Readable 1.0 — keepNames 转写（存档）

> 📌 **本目录已被 [2.0](../readable-2.0/) 接替**，仅作存档保留：2.0 在此基础上补齐了局部变量语义名、Node 别名还原、常量命名与叙事注释，并修复一处转写损坏（见 [2.0 README 的修正对照](../readable-2.0/README.md#相对-10-的修正与改动全部经骨架校验核对)）。
>
> 新读者请从 **[readable-2.0](../readable-2.0/README.md)** 进入；本版 README 的 locals 对照表已被 2.0 吸收。引用、取证仍以 `../01-*.js` … `../08-*.js` minified 逐字摘录为准 —— 1.0 与 2.0 都只是走读副本。

走读副本，**不是 ZCode 原文**。引用、取证、对照用右侧原文摘录。

Human + agent reading copy. Names restored from esbuild `a(fn, "name")`. Locals `e/t/r/…` stay compiler names. Not executable.

Source: ZCode Desktop 3.12.2 `app.asar` → `out/host/index.js` (build `4e1c9d87`). Regenerated by `tools/extract_snapshot.py` + `tools/beautify_snapshot.py` (Grok Build).

## Cite the original

| 走读副本 | 原文（minified 摘录，引用这个） |
| --- | --- |
| `01-sidecar-service.js` | [`../01-sidecar-service.js`](../01-sidecar-service.js) |
| `02-archive-writer.js` | [`../02-archive-writer.js`](../02-archive-writer.js) |
| `03-encrypt-archive.js` | [`../03-encrypt-archive.js`](../03-encrypt-archive.js) |
| `04-upload-client.js` | [`../04-upload-client.js`](../04-upload-client.js) |
| `05-oss-post-object.js` | [`../05-oss-post-object.js`](../05-oss-post-object.js) |
| `06-upload-worker.js` | [`../06-upload-worker.js`](../06-upload-worker.js) |
| `07-pending-manager.js` | [`../07-pending-manager.js`](../07-pending-manager.js) |
| `08-credential-parsing.js` | [`../08-credential-parsing.js`](../08-credential-parsing.js) |
| `09-related.js` | 无单独摘录；引用 `app.asar` → `out/host/index.js`，并回链 01–08 |

## Read order

| file | stage | start here |
| --- | --- | --- |
| `01-sidecar-service.js` | every prompt | `captureBeforePrompt` → `captureBeforePromptUnsafe` |
| `02-archive-writer.js` | pack | `buildRepoSnapshotDelta`, `writeRepoSnapshotPlainArchive` |
| `03-encrypt-archive.js` | envelope | `encryptArchive` — AES-256-CTR + RSA-OAEP |
| `04-upload-client.js` | credential | `getUploadKey`, `requestUploadTarget` |
| `05-oss-post-object.js` | OSS | `uploadPostObject` filename `repo-snapshot.tar.gz.enc` |
| `06-upload-worker.js` | state machine | `flushActiveUpload` |
| `07-pending-manager.js` | queue | `markAcceptedManifest` writes `lastAcceptedManifestHash` |
| `08-credential-parsing.js` | response shape | `data.oss` / `encryption` / `callback` |
| `09-related.js` | helpers | scan, quota, global-config extra lane |

Narrative: `docs/02-code-flow.md`.

## `captureBeforePromptUnsafe` locals

| ident | meaning |
| --- | --- |
| `t` | capture intent (`workspacePath`, `content`, `signal`, …) |
| `r` | login token |
| `o` | `createdAt` |
| `n` | workspace key |
| `i` | workspace key hash |
| `s` | upload key (RSA pubkey + snapshot id) |
| `c` | pending state at turn boundary |
| `l` | max encrypted bytes |
| `p` | extra-file inputs (MCP / AGENTS.md / skills / hooks) |
| `f` | workspace scan `{files, manifest}` |
| `m` | extra-lane scan |
| `A` | `prompt.json` payload (includes user `content`) |
| `L` | `"baseline"` \| `"increment"` |

## Remaining minified helpers (Node)

`y_e` mkdir · `w_e` dirname · `v_e` randomBytes · `Xst` createCipheriv · `ect` createWriteStream · `nct` pipeline · `b_e` createReadStream · `Qst` publicEncrypt · `Jst` crypto.constants · `tct` stat
